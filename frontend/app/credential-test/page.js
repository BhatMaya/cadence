import CredentialStrategyTest from '../../components/CredentialStrategyTest.js';

export const metadata = {
  title: 'Credential timing test - Synergyze',
  description: 'Local Cadence model scoring test page.'
};

export default function Page() {
  return <CredentialStrategyTest />;
}
